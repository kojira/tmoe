//! tmoe CLI/TUI エントリポイント (スタブ)。Phase 6 で ratatui を導入する。

fn main() -> anyhow::Result<()> {
    println!("tmoe v{} — phase-0 skeleton", env!("CARGO_PKG_VERSION"));
    println!("3 エージェントが平面を張り、ユーザーが Z 軸推進力として加わる構造で動く。");
    Ok(())
}
